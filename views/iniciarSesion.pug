<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar sesión en UpTask

            if error
                p.alerta.error= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") Email:
                    input(type="text" name="email" placeholder="Ingresa tu email" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesión")
                .campo
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta? Crea una gratis